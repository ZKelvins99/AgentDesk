# AgentDesk 缺陷审计报告

> **性质**：只读审计报告，不含任何代码改动。对 `apps/desktop/src`（main / preload / renderer）+ `packages/ipc` 全量审计。
> **方法**：进程生命周期链（sidecar / pool / runtime / session-manager / kernel / pty）由主审逐行阅读；安全、渲染层、IPC 契约三块由并行专项审计 + 主审复核关键行号。
> **分级**：**恶性** = 安全绕过 / 崩溃 / 进程泄漏 / 数据错乱；**高** = 常规路径出错或体验崩坏；**中** = 条件苛刻或影响有限。
> 标注 `[复核]` 的条目已由主审亲自读过源码确认；其余条目的行号来自专项审计并已抽查。

---

## A. 恶性问题（10 项）

### A1. 「始终允许」一条 bash 命令 → 放行该会话后续全部命令（审批绕过）`[复核]`

- **位置**：`main/approval/approval-engine.ts` L53-60、L96-105
- **根因**：用户点「始终允许」时保存的规则 matcher 只有 `{ tool }`（L100），不带任何参数约束；而 `decide()` 里**规则匹配先于风险分级**。于是对一条无害的 `npm test` 点了始终允许后，`{tool:'bash'}` 会命中该会话后续**所有** bash 命令——包括 `rm -rf ~`。README「高危任何模式都询问」的保证被架空。
- **触发**：auto-edit / full-access 下对任意 bash 命令选过一次「始终允许」。
- **修法**（两条都要做）：
  1. `decide()` 的规则命中分支加保险：`if (rule.decision === 'allow' && risk === 'high') 跳过该规则，继续走询问`。
  2. 保存 always 规则时带参数约束：
     ```ts
     matcher:
       tool === 'bash' ? { tool, bashPrefix: cmd.split(/\s/)[0] }
       : ['write', 'edit'].includes(tool) ? { tool, pathPrefix: path.dirname(resolvedPath) }
       : { tool }
     ```
  3. 补单测：always 规则 + high risk 命令 → 必须返回 ask。

### A2. 审批弹窗的全局键盘监听无焦点隔离 —— 打字即批准 `[复核]`

- **位置**：`renderer/src/components/ApprovalModal.tsx` L49-60
- **根因**：只要存在待审批项就在 `window` 上挂 keydown：`Enter → allow-once`、`a → always`、`d → deny`，**完全不检查 `e.target`**。用户在 Composer 里打含有 a/d 的文字、或按回车发送消息时，会**同时**批准（或拒绝）高危工具调用。审批形同虚设。
- **触发**：弹出审批卡 → 用户在输入框打字 / 按回车发送。
- **修法**：
  ```ts
  const onKey = (e: KeyboardEvent) => {
    const t = e.target as HTMLElement | null;
    if (t && (t.tagName === 'INPUT' || t.tagName === 'TEXTAREA' || t.isContentEditable)) return;
    // ...原有逻辑
  };
  ```
  更好的做法：审批卡打开时把焦点移入卡内，只在卡片内响应快捷键；并给 `use-keyboard.ts` 的 `⌘⏎ 批准 / ⌘⌫ 拒绝` 加同样的输入框排除（README 9.8 里这两组键也受影响）。加一条回归测试：`document.activeElement` 是 textarea 时按 Enter 不触发 resolveApproval。

### A3. 关闭终端面板不 kill pty、不 dispose xterm —— 进程 + 内存双泄漏 `[复核]`

- **位置**：`renderer/src/components/TerminalPanel.tsx` L26（模块级 `terminalMap`）、L52-54（挂载 effect 无 cleanup）、全文件无卸载清理
- **根因**：点面板右上角 ×（`onClose`）只隐藏面板，`tabs` state 随组件卸载丢失，但主进程侧的 pty **没有被 kill**、xterm 实例永久滞留模块级 Map（计时器/监听器/DOM 全泄漏）。重开面板 `createTab` 又新建 pty。唯一清理路径是用户手动逐个关 tab（`closeTab`）或应用退出（`killAll`）。
- **触发**：开终端 → 关面板，反复 N 次 = N 个孤儿 shell 进程。
- **修法**：
  ```ts
  const tabsRef = useRef<TerminalTab[]>([]);
  useEffect(() => { tabsRef.current = tabs; }, [tabs]);
  useEffect(() => () => {
    for (const tab of tabsRef.current) void window.agentdesk.pty.kill({ ptyId: tab.ptyId });
    for (const [id, entry] of terminalMap) { entry.terminal.dispose(); terminalMap.delete(id); }
  }, []);
  ```
  （或：关面板时不销毁组件，改为 CSS 隐藏——但须先评估 xterm 后台驻留的内存成本。）

### A4. 事件广播不检查窗口销毁状态 —— 关窗瞬间主进程崩溃 `[复核]`

- **位置**：`main/session/runtime.ts` L133-135（`event:update`）、L210-212（`event:resources`）、L264-266（`event:session`）
- **根因**：三处 `BrowserWindow.getAllWindows()` 循环内直接 `win.webContents.send(...)`，无 `isDestroyed()` 检查（对照组：`main/pty/pty-service.ts` L73 就有正确检查）。向已销毁窗口 send 抛 `Object has been destroyed`，且发生在 sidecar/uplink 事件回调里，属**未捕获异常**。`event:session` 是流式高频通道（每个 msg.delta 都广播），关窗竞态极易命中。
- **修法**：统一封装并替换三处：
  ```ts
  function broadcast(channel: string, payload: unknown): void {
    for (const win of BrowserWindow.getAllWindows()) {
      if (!win.isDestroyed() && !win.webContents.isDestroyed()) win.webContents.send(channel, payload);
    }
  }
  ```

### A5. sidecar 崩溃重启链断裂：重启的进程永不回收，重启耗尽后无法恢复 `[复核]`

- **位置**：`main/pi/sidecar-pool.ts` L122-145、L149-160；`main/session/session-manager.ts` L351 / L428
- **根因**：一条完整的断链——
  1. `waitReady()`（把 status 从 `starting` 翻到 `ready`）**只在 `SessionManager.create/resume` 里调用**（session-manager.ts L351/L428）；
  2. 但崩溃重启由 `SidecarPool.scheduleRestart` 自己 `new PiSidecar + start()`（pool L144），**从不 waitReady**；SessionManager 的 `sidecar-created` 监听只做 `rt.attach(sidecar)`，也不 waitReady；
  3. `sidecar.once('event', ...)` 里靠 `status === 'ready'` 清零重启计数（pool L140）→ **永不为真**，计数永不复位；
  4. `reclaimIdle` 只回收 `status === 'ready'` 的进程（pool L153）→ 重启出来的进程 status 停在 `starting`，**空闲回收永远不会碰它**——每次崩溃重启都净增一个常驻进程；
  5. 重启次数耗尽（`restart-exhausted`）后 pool 删掉条目，但 `SessionRuntime` 还留在 `runtimes` 里引用已死的 sidecar；此后每次 `send` 只报「会话未在运行，请先 attach」，而 `attach` 又因 `runtimes.has()` 直接返回旧快照，**永远不会重新 resume** → 会话永久卡死，用户唯一出路是重启应用。
- **修法**：
  1. `scheduleRestart` 的 setTimeout 回调里 `sidecar.start()` 后补 `void sidecar.waitReady(15_000).catch(...)`；或把 `sidecar-created` 监听里改成 `rt.attach(sidecar); void sidecar.waitReady(...)`（二选一，避免重复）；
  2. `restart-exhausted` 时 SessionManager 应 `this.runtimes.delete(sessionId)` 并推送错误事件，让渲染层能走「删除/重建」或触发 `attach → resume` 重新拉起；
  3. 补集成测试：模拟 sidecar 连续崩溃 4 次，断言第 5 次 `attach` 能重新拉起、且 pool.size 不随重启增长。

### A6. 全局单一 uplink token 注入所有 sidecar，`/mcp/call` 完全绕过审批引擎

- **位置**：`main/approval/uplink-server.ts` L117 / L313-366；`main/session/session-manager.ts` L595-601 `[复核]`
- **根因**：token 每进程仅一个，所有会话 sidecar 通过 `AGENTDESK_TOKEN` env 拿到**同一** token。sidecar 再 spawn 的工具子进程（bash）继承 env——任何被 agent 执行的命令 `printenv` 即可读走 token，随后：调 `POST /mcp/call` **直接执行任意 MCP 工具（该路径不过 ApprovalEngine）**、以任意 sessionId 请求 `/approval`、伪造 `/state/resources` 广播到所有窗口。`handleApproval` 的 sessionId/cwd 均来自请求体（L245/L256），无会话绑定校验。
- **触发**：一次恶意或被注入的 shell 命令读取环境变量。
- **修法**：
  1. 每会话生成独立 token：`Map<token, sessionId>`，`authorized()` 返回绑定的 sessionId，各 handler 强制 `body.sessionId === boundSession`；
  2. `handleMcpCall` 先经 `engine.decide({ tool: `mcp__${server}__${tool}`, ... })`；
  3. 评估用文件（0600 权限）而非 env 传 token，避免子进程继承。

### A7. `before-quit` 不 await dispose —— 退出时优雅清理全部失效 `[复核]`

- **位置**：`main/index.ts` L54-56
- **根因**：`app.on('before-quit', () => { void runtime.dispose(); })` —— `void` 掉了 Promise。Electron 不会等它完成，进程随即退出。`dispose` 里的 `shutdownAll(5s)`、`killProcessTree` 逐级升级、uplink `close()`、`db.close()` 全部来不及执行。**Windows 上 sidecar / pty 子进程不随父进程自动终止 → 应用退出后残留一堆 pi 进程和 shell 进程**（还带着 provider 密钥 env）。
- **修法**：
  ```ts
  app.on('before-quit', (e) => {
    if (!disposing) {
      disposing = true;
      e.preventDefault();
      void runtime.dispose().finally(() => app.exit(0));
    }
  });
  ```
  并给 dispose 整体套一个 8s 上限 `Promise.race`，防止 dispose 自身挂死导致退不出去。另可考虑 Windows 上用 Job Object 绑定子进程（治本）。

### A8. `kernel:update` 接受任意字符串 → 路径穿越 + 供应链投毒 `[复核]`

- **位置**：`packages/ipc/src/contracts.ts` L66-69（`version: z.string().optional()` 无约束）；`main/kernel/kernel-manager.ts` L235-246
- **根因**：`version` 直接拼进两处：GitHub URL `/releases/tags/v${version}`（L302）和本地路径 `path.join(kernelsDir, 'versions', target)`（L238）。传 `../../../tmp/evil` 即可把解压产物写到 `~/.agentdesk/kernels` 之外，并 `setActive` 激活。**激活的内核会被所有会话 spawn，且每个 sidecar 都带着 provider 密钥 env**——等价于任意代码执行 + 密钥窃取。渲染层虽可信，但纵深防御要求边界自校验（README 16.1）。
- **修法**：
  ```ts
  version: z.string().regex(/^\d+\.\d+\.\d+(-[\w.-]+)?$/).optional()
  ```
  另在 `fetchTo` 开头加 `if (target !== path.basename(target)) throw`。

### A9. AgentDeskError 跨 IPC 序列化丢失字段 —— 错误契约在边界处整体失效

- **位置**：`packages/shared/src/errors.ts`；`main/ipc.ts` 绝大多数 handler
- **根因**：Electron invoke 的错误经 structured clone，只保留 `name/message/stack/cause`，`AgentDeskError` 的 `code` / `scope` / `userMessage` 实例字段全部丢失。渲染层拿到的是拼串后的普通 Error，**无法按 code 分支处理**（这正是上一轮「RPC prompt 失败」只能靠字符串剥壳兜底的根源）；服务层抛的原生 Error 也直接透传。
- **修法**：注册统一包装器，所有 handler 走它：
  ```ts
  function handle(channel: InvokeChannel, fn: (req: unknown, ev: IpcMainInvokeEvent) => unknown) {
    ipcMain.handle(IPC_CHANNELS[channel], async (ev, raw) => {
      try {
        return await fn(parseRequest(channel, raw), ev);
      } catch (err) {
        const e = err instanceof AgentDeskError ? err : AgentDeskError.from(err);
        throw Object.assign(new Error(e.message), {
          name: 'AgentDeskError', code: e.code, scope: e.scope, userMessage: e.userMessage,
        });
      }
    });
  }
  ```
  渲染层 `error-message.ts` 相应优先读 `err.userMessage`，字符串剥壳逻辑降级为兜底。顺带可消灭 A 清单之外「十余个 handler 无 try/catch」和「无 payload handler 未过 Zod」两个系统性缺口（统一入口强制 parseRequest）。

### A10. attachSession 用过期快照整体覆盖 —— 流式中切会话丢消息 `[复核]`

- **位置**：`renderer/src/stores/session-store.ts` L78-92（对照 L229-245 的乱序保护）
- **根因**：`attach` await 期间，实时事件仍经 `applyEvents` 落库并推进 `appliedSeq`；await 返回后用快照重放结果**整体替换**该会话 state 并强置 `appliedSeq = snap.seq`。快照时点之后已应用的增量**永久丢失**（后续同 seq 事件被 L236 的 `p.seq <= cur.appliedSeq` 拦掉）；本地乐观用户消息（`send` 的 addUserMessage）也可能被抹掉。
- **触发**：流式输出中用 `⌘⌥←/→` 切会话（`use-keyboard.ts` L98 每次 `sinceSeq=0` 重连）；新建会话时事件竞态。
- **修法**：
  1. 调用处改用增量：`attachSession(id, get().sessions[id]?.appliedSeq ?? 0)`；
  2. `attachSession` 内合并而非覆盖：重放时跳过 `snap.history` 中 `seq <= cur.appliedSeq` 的事件，且 `appliedSeq` 取 `max(snap.seq, cur.appliedSeq)`，保留 `cur` 中未在快照里的本地消息。

---

## B. 高危问题（8 项）

### B1. `setWindowOpenHandler` 未校验 URL scheme 即 `shell.openExternal` `[复核]`

- **位置**：`main/windows.ts` L32-35
- **根因**：页面任意 `window.open` / `<a target=_blank>` 的 URL 原样交给系统。`vscode:`、`ms-word:` 等自定义协议可拉起外部程序；`javascript:` 等会产生未处理的 Promise rejection（`void` 吞掉了）。渲染内容含 Markdown 渲染与 extension `ui.request` 注入的链接，攻击面真实存在。
- **修法**：
  ```ts
  win.webContents.setWindowOpenHandler(({ url }) => {
    try {
      const u = new URL(url);
      if (u.protocol === 'http:' || u.protocol === 'https:' || u.protocol === 'mailto:') {
        shell.openExternal(u.toString()).catch(() => {});
      }
    } catch { /* 非法 URL 忽略 */ }
    return { action: 'deny' };
  });
  ```

### B2. 风险分级启发式缺口 + 默认 full-access → 危险命令静默放行

- **位置**：`main/approval/risk.ts` L41-50；`main/session/runtime.ts` L243-249
- **根因**：`git push -f`（只匹配 `--force`，漏 `-f` / `--force-with-lease`）、`curl ... | python/perl`（只匹配 `| sh/bash`）、Windows `del /s`、`Remove-Item -Recurse`、`find -delete` 均判为 low/medium；而默认审批模式是 `full-access`，非 high 直接 allow，全程无弹窗。
- **修法**：`isHighBash` 增补正则：`git\s+push\b[^|]*(--force|-f\b|--force-with-lease)`、`\|\s*(python3?|perl|ruby|node)\b`、`(^|\s)(del|rmdir)\b.*\/s`、`Remove-Item\b.*-(Recurse|r\b)`、`\bfind\b[^|]*-delete`；并把默认模式改为 `auto-edit`（README 11 的安全基调更匹配）。

### B3. abort 无本地兜底 —— UI 可永久卡在 streaming `[复核]`

- **位置**：`renderer/src/stores/session-store.ts` L219-227；主进程侧 `main/session/session-manager.ts` L454-457（5s 超时即 reject）
- **根因**：abort 仅 await IPC 并吞错；状态复位完全依赖内核发 `agent.settled` / `session.state`。任何一环失败（内核无响应、IPC 超时），Composer 永远显示「停止」按钮，发送按钮不回来。
- **修法**：abort 后启动 3s 兜底定时器，若届时 `state.isStreaming` 仍为 true，主动调一次 `session:context-usage` 或新增 `session:get-state` 校正；仍为 true 则乐观置 idle 并落一条系统消息。`message-model.ts` 的 `turn.end` 也应在无 pending 时把 streaming → idle。

### B4. restore 失败后永久失效 + 未处理 rejection `[复核]`

- **位置**：`renderer/src/stores/session-store.ts` L46、L99-115；`renderer/src/App.tsx` 的 `void restore()`
- **根因**：模块级 `restoreStarted` 置 true 后**从不复位**，也无 catch。`loadSessions` 一旦 reject（IPC 异常），成为未处理拒绝，且用户无任何途径重试，应用停在空白态。
- **修法**：`finally` 里 `restoreStarted = false`；catch 后 `set({ restoreError })` 供 UI 显示重试按钮。

### B5. fork 出的会话无法使用：DB 记录缺 sessionFile，永远拉不起 sidecar `[复核]`

- **位置**：`main/session/session-manager.ts` L708-731（对照 L387 的 resume 条件）
- **根因**：`fork()` 调 pi `fork_session` 拿到新 piSessionId 后，只写 DB 记录，**不记录 sessionFile**。而 `attach` 的后台恢复条件是 `record.sessionFile && archivedAt === null`（L387）→ fork 会话永远不 resume sidecar → 打开后 `send` 报「会话未在运行」。fork 功能实际不可用。
- **修法**：fork 后向 pi `get_state` 取新会话的 sessionFile 写入记录；或 fork 时直接把新会话作为新 runtime 拉起（`createSessionSidecar` + `--session <新文件>`）。补集成测试覆盖「fork → 切到新会话 → send 成功」。

### B6. 路径判定不解析符号链接 —— 工作区内 symlink 逃逸审批 `[复核相关代码]`

- **位置**：`main/approval/approval-engine.ts` L131-149；`main/approval/rules.ts` L54-60
- **根因**：`isPathInside` / pathPrefix 均用 `path.resolve`（词法归一化），不解析 symlink。工作区内放 `lnk -> ~/.ssh`，auto-edit 模式对 `write lnk/authorized_keys` 自动放行；pathPrefix 白名单同理被绕。
- **修法**：判定前尽力 `fs.realpathSync.native`（失败则逐级对父目录解析），对解析后的真实路径做包含判断。

### B7. SessionRuntime.history 内存无上限增长 `[复核]`

- **位置**：`main/session/session-manager.ts` L153、L241（`handle` 每事件 push）
- **根因**：M3 起事件已同步落 SQLite，但内存 `history` 数组从不收缩。长会话（数千事件）主进程内存持续增长；`snapshot()` / `eventsSince()` 还会全量复制/过滤它。
- **修法**：内存只保留尾部窗口（如最近 2000 条 + seqOffset），`eventsSince` 对超出窗口的 sinceSeq 回落 DB（`store.getEventsSince` 已存在，直接复用）。

### B8. 审批弹窗只发给第一个窗口 + pending Map 泄漏

- **位置**：`main/ipc.ts` L333-341、L343-354 `[复核]`
- **根因**：① `BrowserWindow.getAllWindows()[0]` —— 多窗口时审批卡可能弹在用户看不见的窗口（且该窗口被销毁后直接返回 timeout）；② `ApprovalEngine` 30s 超时后 race 结束，但 `pendingApprovals` 的 Map 条目与 pending Promise **永不释放**；③ `approval:respond` 遇未知 requestId 静默 return，渲染层弹窗无从感知已过期。
- **修法**：① 广播所有窗口或发给 `event.sender` 所属窗口；② ipc.ts 侧自建超时定时器：超时时 `pendingApprovals.delete(req.id)` 并推送 `event:approval-expired` 让渲染层关弹窗；③ respond 找不到条目时返回 `{ expired: true }`。

---

## C. 中等问题（10 项）

| # | 位置 | 问题 | 修法要点 |
|---|---|---|---|
| C1 | `main/config/config-store.ts` L232-264 | models.json 原始编辑器可把 `apiKey` 明文写盘（schema 对 providers 是 `z.unknown()` passthrough），绕开 safeStorage+`$VAR` 规格 | `save()` 时扫描 `providers[*].apiKey`：非 `$` 开头且具密钥特征 → 拒绝保存并提示走密钥管理，或自动转存 SecretsStore 替换为 `$AGENTDESK_KEY_*` |
| C2 | `main/logging/logger.ts` L79-84；`main/telemetry/diagnostic.ts` L87-98 | pino `redact` 只遮字段路径，`msg` 字符串原样落盘；脱敏只在导出时发生；两处 `sk-` 阈值不一致（16 vs 8） | pino 加 `formatters.log` 对 msg 套 `redactText`；阈值统一为 8 |
| C3 | `main/packages/package-security.ts` L243-302；`package-manager.ts` L491-520 | 审查与安装两次独立拉取（TOCTOU）：npm 审查 `latest`、git 审查分支 ref，`pi install` 再拉一次，内容可被替换；install 不校验是否审查过 | `inspected: Map<identity, hash>`；npm 固定审查时解析的精确版本、git 记录审查时 HEAD commit 并按 commit 安装 |
| C4 | `main/pty/pty-service.ts` L153-177 | ① README 12.4 要求「shellPath 配置优先」未实现（只有硬编码候选）；② `spawnSync('wsl.exe --status', 2s)` **同步阻塞主进程**，每次创建 pty 都执行 | 读 config 的 shellPath 置顶；WSL 探测结果缓存 + 改异步（`execFile` Promise） |
| C5 | `main/pty/pty-service.ts` L34-38 | `getAllWindows()[0]` 取窗口：多窗口时 pty 输出只发给第一个窗口 | PtyService 持有目标窗口引用（create 时传入），或广播全部存活窗口 |
| C6 | `renderer/src/components/Composer.tsx` L32/L38-47；`SessionView.tsx` L53 | Composer 跨会话复用 → 草稿串流；`send` 无重入守卫，双击发送可产生重复消息 | `<Composer key={activeId} />`；submit 加 `sendingRef` 锁，store 的 `send` 开头查 `isStreaming` 走 steer 已部分缓解，但 normal 模式仍需锁 |
| C7 | `renderer/src/stores/ui-store.ts` L149-158 | `cycleApprovalMode` 只改 ui-store + 发 IPC，不同步 session-store 的 `approvalMode` → Composer 徽标滞后；初值 null 使首次循环强制落到 plan | cycle 后调 `useSessionStore.getState().setSessionApprovalMode`；或审批模式只存 session-store 一处，ui-store 订阅 |
| C8 | `renderer/src/components/ModelPicker.tsx` L36-50 | getModels 无 cancelled 标记：开面板时切会话，旧响应覆盖新列表；`pick` 按当前 activeId 可能把 A 会话的模型写到 B | 请求带 sessionId 闭包，响应时比对当前 activeId；或 AbortController 取消旧请求 |
| C9 | `main/ipc.ts` L865-872 | `app:update-install` state≠downloaded 时静默返回 undefined；`app:open-logs` 忽略 `shell.openPath` 返回值（失败时它 resolve 错误信息字符串） | update-install 返回 `{ installed: boolean }`；open-logs 检查返回值非空否则抛 AgentDeskError |
| C10 | `main/session/runtime.ts` L155 | 默认 workspace 用 `process.cwd()` —— 打包后取决于启动方式，可能是 `C:\` 或安装目录，会话 cwd 与文件树根都落在那里 | 无显式 workspace 时引导用户选择（onboarding 已存在），或默认 `~/AgentDesk` 并自动创建 |

---

## D. 卫生问题（小但应清）

| # | 位置 | 问题 |
|---|---|---|
| D1 | `main/session/session-manager.ts` L651 / L658 | 注释编码错乱：「用 DB 浏誈输入 token」「卸个限 80%」——又一处编码处理事故残留（全库已无 U+FFFD，但此类错字还在）。应重写为「用 DB 累计输入 token 估算」「阈值取 80%」 |
| D2 | `main/ipc.ts` L310 / L313 | `[debug:get-models]` console.log/error 调试残留，会进主进程日志 |
| D3 | `renderer/src/components/TerminalPanel.tsx` L166-186 | **`<button>` 里嵌 `<button>`**（tab 关闭按钮嵌在 tab 按钮内）——无效 HTML，DOM 解析会拆开，点击行为异常。改为 `<div role="tab">` 包两个 button，或关闭按钮移出并绝对定位 |
| D4 | `renderer/src/components/TerminalPanel.tsx` L86-94 | xterm theme 硬编码旧色板（`#0d0d0f` / `#4d9fff`），不随深浅主题切换（与《前端现代化开发提示词.md》色彩统一任务合并处理） |
| D5 | `main/windows.ts` L17 | `backgroundColor: '#0d0d0f'` 旧色板，窗口打开瞬间闪旧色；应为 `#212121`（dark）/ 随主题 |
| D6 | `renderer/src/stores/session-store.ts` L128-135 | `setSessionApprovalMode` 中 `void window.agentdesk.session.setApprovalMode(...)` 失败静默 |
| D7 | preload `session.create`（`preload/index.ts` L45-46） | 手写请求类型缺 `provider` / `approvalMode` 字段（对照 `InvokeMap['session:create']`），创建会话时无法指定 provider。改用 `InvokeMap['session:create']['request']` |
| D8 | `event:kernel` 通道 | preload L241-242 有订阅、contracts.ts L1557 有定义，但**全 main 无任何生产者**。要么在 `kernel:update/rollback` 后广播，要么删除该通道 |
| D9 | `.npmrc-build-tmp` / `apps/desktop/tmp-check-secret.cjs` | 前者已误提交进 git（需 `git rm --cached`），后者未追踪残留 |

---

## 建议修复顺序

1. **第一批（安全，半天内）**：A1、A2、A6、A8、B1、B2 —— 全部是审批/供应链/外部协议边界，任何一条被利用都是灾难性的。
2. **第二批（进程生命周期，一天）**：A4、A5、A7、A3 —— 崩溃与泄漏主力；A5 改动面稍大（pool + session-manager 联动），务必配集成测试。
3. **第三批（数据正确性）**：A9、A10、B3、B4、B5 —— A9 是系统性工程（统一 handler 包装器），做完后渲染层错误处理会明显变干净。
4. **第四批**：B6-B8、C 清单、D 清单按序清。

**总验收**：`pnpm typecheck && pnpm test` 全绿（当前 41 文件 / 256 用例，不许变红）；A1/A2/A5/A10 各至少一条新回归测试；`git` 提交按上面批次分组，一次提交一个批次。
