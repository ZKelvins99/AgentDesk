# AgentDesk 前端现代化 · 开发提示词

> **本文件的定位**：这是一份交给「后续开发模型」的**完整任务提示词**，不是设计文档，也不是进度清单。
> 上游依据：[README.md](README.md)（产品与技术规格，18 章）、[建设步骤.md](建设步骤.md)（里程碑落地清单，M0–M9 已全部完成）。
> 本轮工作的性质：**M0–M9 功能骨架已闭合，现在做一轮跨越全部界面的「统一化 + 平台适配 + 缺陷收口」**。它不新增业务功能。

---

## 0. 给接手模型的元指令（先读这段）

你接手的是一个**功能已经跑通、但视觉与交互一致性欠债严重**的 Electron 桌面端。用户对现状的原话是：

> 「UI 一定要现代化好看，各方面的 UI 统一，字体统一没有字体挤在一起的情况，色彩统一。快捷键以及图标分 windows 和 mac 两套，自动检测。功能上没有 bug。」

请按下面的方式工作：

1. **先读第 1 节的「事实基线」再动手**。README 第 9 章（UI/UX 规格）的部分条目**已经与代码实现脱节**，第 1.3 节列出了全部脱节项。若你照 README 9.1 去"修正"代码，会把已经做好的现代化改动改回旧样式。
2. **本文档中的所有数字都是实测得来的**（命令见第 6 节），不是估算。你整改后应让这些数字往目标值收敛，并用同样的命令自证。
3. **不要一次性重写 `styles.css`**。它有 3189 行，一次性重写必然引入回归。按第 2 节的分组，一组一提交。
4. **区分「事实」与「待你决策」**：标注 `[已验证]` 的是我实测确认过的；标注 `[需你确认]` 的是我未能验证、需要你在实现前先查证的。不要把 `[需你确认]` 当既定事实。
5. **凡涉及用户可见行为的改动，必须同步更新 README 对应章节**（README 16.3 DoD 第 6 条）。本轮尤其是 README 9.1 / 9.8。

---

## 1. 事实基线（必读，防止返工）

### 1.1 代码位置

| 关注点 | 路径 |
|---|---|
| 设计 token（唯一色彩/字体来源） | `packages/ui/src/theme/tokens.css`（93 行） |
| 渲染层全部样式 | `apps/desktop/src/renderer/src/styles.css`（3189 行，**单文件**） |
| SVG 图标组件 | `apps/desktop/src/renderer/src/components/Icon.tsx`（31 个图标，单套） |
| 快捷键**绑定** | `apps/desktop/src/renderer/src/hooks/use-keyboard.ts`（114 行） |
| 快捷键**显示文案** | `apps/desktop/src/renderer/src/utils/shortcut.ts`（12 行） |
| 窗口装饰 | `apps/desktop/src/main/windows.ts` |
| i18n | `apps/desktop/src/renderer/src/i18n/index.ts`（zh-CN 225 键 / en 225 键） |

### 1.2 最近一次 UI 改动（commit `e8acffb`）已完成的事

这些**不要重做**，在此基础上继续：

- 移除双标题栏：`frame: false` + `titleBarStyle` + `Menu.setApplicationMenu(null)`
- 31 个单色线性 SVG 图标替换全部 emoji（emoji 在 Windows 上字形退化）
- 色板从蓝调改为 codex 中性灰；新增 `--btn-primary-bg/-fg` 反色实心主按钮
- 消除界面上的 `unknown`：pi 未配供应商时返回的是**字符串 `"unknown"`** 而非 `null`，故 `??` 兜底无效；已由 `utils/model-label.ts` 的占位 id 集合收口
- 模型徽标从不可点的 `<span>` 改为 `<button>`，接通已存在的 `ModelPicker`
- 裸露的 Electron IPC 错误（`Error invoking remote method 'session:send': ...`）已由 `utils/error-message.ts` 剥壳为友好文案
- 补齐 `CommandPalette` / `GlobalSearch` / `ContextUsageDrawer` 三个弹层的样式——**它们此前完全没有任何 CSS**，打开就是无样式裸元素

### 1.3 ⚠ README 9.1 与实现的脱节清单 `[已验证]`

README 9.1 是 M0 时写的规格，`e8acffb` 已按 codex 视觉重做但**未回写 README**。当前差异：

| 维度 | README 9.1 写的 | 代码实际 | 处理方式 |
|---|---|---|---|
| 色板 | `--bg-app: #0d0d0f`、`--accent: #4d9fff` | `#212121`、`#6ea8fe` | **以代码为准**，改 README |
| 基准字号 | 13px | 14px | **以代码为准**，改 README |
| 圆角 | 卡片 10 / chip 8 / 气泡 12 | 卡片 12 / chip 8 / 气泡 18 | **以代码为准**，改 README |
| 密度 | 侧栏行高 28px、消息间距 12px | 更宽松 | **以代码为准**，改 README |
| 标题栏高度 | 32px | 36px | **以代码为准**，改 README |
| 自绘菜单栏 | 要求「文件/编辑/视图/帮助」 | **不存在**，只是移除了原生菜单 | 见第 4.4 节，需决策 |

> 处理原则：README 9.1 的**具体数值**以代码为准回写；README 9.1 的**原则性要求**（焦点环 2px 不可 `outline:none`、`prefers-reduced-motion` 降级、正文最大 76ch）仍然有效，且第 2 节要求你逐条核查落实情况。

### 1.4 量化欠债（实测，第 6 节可复现）

| 问题 | 实测值 | 目标 |
|---|---|---|
| `styles.css` 硬编码 `font-size` | **151 处**，仅 17 处走 token | 0 处硬编码 |
| 字号取值种类 | **12 种**（10 / 11 / 11.5 / 12 / 12.5 / 13 / 14 / 14.5 / 15 / 16 / 18 / 20 / 22） | 收敛到 ≤6 级阶梯 |
| 间距声明（`gap`/`padding`/`margin`） | **322 处，仅 1 处走 var()** | 全部走 `--space-*` |
| `--space-*` / `--gap-*` token | **0 个**（根本没定义） | 建立 4px 基准阶梯 |
| 硬编码 16 进制颜色 | **34 处** | 0 处（全部走语义 token） |
| `line-height` 显式声明 | 8 处，其中 **3 处是 `line-height: 1`** | 见第 2.2 节，CJK 下危险 |

**这三项就是用户所说「UI 不统一 / 字体挤 / 色彩乱」的直接技术根因**，不是主观审美问题。

### 1.5 工程状态

- `pnpm typecheck` → **6/6 通过** ✅
- `pnpm test` → **41 文件 / 256 用例全通过** ✅
- `pnpm verify:i18n` → zh-CN 225 : en 225 对称 ✅（另有 **255 处历史裸中文**警告，非阻塞）
- `pnpm lint` → **17 个 format 错误**，全部在 `kernel-manager.ts` / `metrics-store.ts` / `env.d.ts` / `OnboardingWizard.tsx` / `UpdateBanner.tsx` / `scripts/check-i18n.mjs` / `scripts/verify-pi-facts.mjs` —— **这些是 `e8acffb` 之前就存在的**，不是新引入的
- 待清理垃圾文件：`.npmrc-build-tmp`（**已被 git 追踪，属误提交**）、`apps/desktop/tmp-check-secret.cjs`（未追踪）

### 1.6 编码红线 `[已验证，必须遵守]`

`styles.css` 曾出现过两行**永久损坏**的中文注释：文件里被写入了 `EF BF BD`（U+FFFD 替换字符本身），原文无法恢复，只能按上下文重写。原因是用错误编码读写中文文件。

因此：
- 读写含中文的文件，**一律显式用 UTF-8 无 BOM**
- **行尾按文件类型区分** `[已验证]`：仓库无 `.gitattributes`，实际现状是
  - 源码（`.ts` / `.tsx` / `.css`）为 **LF**，Biome 配置 `lineEnding: lf`
  - 根目录 Markdown（`README.md` / `建设步骤.md` / 本文件）为 **CRLF**

  改文件时**保持该文件原有行尾**。不要用 `Environment.NewLine` 之类依赖平台的换行写文件——在 Windows 上它是 CRLF，会把 LF 源码文件整体翻转，导致 diff 显示"全文件重写"而淹没真实改动（这个坑上一轮踩过）。同样地，也**不要**把现有 CRLF 的 Markdown 转成 LF，那会凭空产生两千行假 diff。
- 改完中文文件后自查：文件内不得出现 U+FFFD

---

## 2. 任务一：设计系统统一（对应「UI 统一 / 字体统一 / 色彩统一」）

### 2.1 建立完整的 token 阶梯

当前 `tokens.css` 只有颜色、字体族、少量圆角，**没有字号阶梯、没有间距阶梯**，所以 151 处字号和 322 处间距各写各的。

在 `packages/ui/src/theme/tokens.css` 的 `:root` 中补齐（数值可微调，但**层级数量不要膨胀**）：

- **字号阶梯**：6 级封顶，例如 `--font-size-xs / sm / base / md / lg / xl`。base 保持 14px。
  - 硬性要求：**全部为整数 px**。当前的 `12.5px` / `11.5px` / `14.5px` 在非整数缩放下渲染发虚，必须消除。
- **行高阶梯**：至少区分 `--line-height-tight`（标题/单行）、`--line-height-base`（正文 1.65）、`--line-height-relaxed`（长文本）。
- **间距阶梯**：以 4px 为基准，`--space-1: 4px` 到 `--space-8: 32px`。当前 322 处间距里出现了大量 3px / 5px / 6px / 9px / 11px / 14px 这类离散值，必须吸附到阶梯上。
- **层级（z-index）**：当前弹层 z-index 是散写的字面量，收口为 `--z-dropdown / --z-overlay / --z-modal / --z-toast`，避免后续弹层互相盖不住。

然后**分组替换 `styles.css`**，每组一次提交，建议顺序（依赖从低到高）：
`titlebar` → `sidebar` → `session-header` → `message-list / msg-bubble` → `composer` → 三个 overlay（`command-palette` / `global-search` / `context-drawer`）→ 设置页与其余面板。

### 2.2 字体统一，消除「字挤在一起」

用户看到的「字体挤在一起」有**四个可叠加的技术原因**，逐个处理：

1. **中英混排时实际用了两套字体** `[已验证]`
   当前 `--font-ui` 为：
   `ui-sans-serif, -apple-system, "Segoe UI Variable", "Segoe UI", Inter, "Noto Sans SC", sans-serif`
   问题：`Segoe UI` **不含 CJK 字形**，`Noto Sans SC` 在 Windows 上**默认未安装**。于是 Windows 上英文命中 Segoe UI、中文跌到最后的 `sans-serif`（通常是微软雅黑/宋体）。两者 x-height、字宽、字重不同 → 混排时基线跳动、字距不匀，**观感就是"挤"**。
   **修法**：在字体链中显式加入各平台的系统中文字体，让中文有确定归属，例如 macOS 的 `PingFang SC`、Windows 的 `Microsoft YaHei UI`，并置于 `sans-serif` 之前。等宽字体链同理需要 CJK 回退。

2. **`line-height: 1` 会让 CJK 上下贴死** `[已验证，3 处]`
   CJK 字形占满 em box，行高 1 时相邻行完全无间隙。这 3 处逐个复核：若是为了图标/单行按钮垂直居中，改用 flex 居中 + 明确 `height`，而不是压行高。

3. **负字距对中文是负优化** `[已验证]`
   `e8acffb` 给 `.titlebar-brand` 等加了 `letter-spacing: -0.01em`（西文标题常见手法），但它同样作用于中文，让中文更挤。**给 CJK 文本不要用负 `letter-spacing`**；确需收紧的，限定在纯西文元素上。

4. **缺少字体渲染设定**
   补 `-webkit-font-smoothing: antialiased`、`text-rendering: optimizeLegibility`，并确认全局 `font-family` 只在 `:root`/`body` 声明一次、其余元素继承（含 `input` / `textarea` / `button`，这三个**不继承** `font-family`，必须显式 `font-family: inherit`，否则会退回浏览器默认字体 —— 这是混排不一致的常见来源，请全局排查）。

**验收**：在 Windows 与 macOS 各截一张同一界面的图，中英混排处字距均匀、基线不跳；全仓 `font-family` 声明点收敛到可枚举的少数几处。

### 2.3 色彩统一

- 消除 `styles.css` 中 **34 处硬编码 16 进制颜色**，全部映射到 `tokens.css` 的语义 token。
- 若发现某颜色**没有**对应语义 token，**不要就地硬编码**，先在 `tokens.css` 增补语义名（如 `--bg-code`、`--border-focus`），再引用。
- **深浅双主题必须对等**：`:root[data-theme="dark"]` 与 `[data-theme="light"]` 的 token 键集合必须完全一致。当前 dark 有 `--shiki-dark/-bg`、light 有 `--shiki-light/-bg`，属于有意的非对称，其余键请核对是否有遗漏（遗漏会导致切主题时某处颜色不跟随）。
- 语义化而非外观化命名：用 `--danger` 而不是 `--red`。审计现有用法是否有"借用"（例如拿 `--warn` 当装饰色用）。
- **对比度**：README 9.9 要求 ≥ WCAG AA。重点核查 `--fg-muted` 在 `--bg-sidebar` / `--bg-input` 上的对比度，以及 `--accent-fg`（dark 下为 `#1a1a1a`）与 `--accent`（`#6ea8fe`）的搭配。逐一实测，不要凭感觉。

### 2.4 组件级尺寸统一

同类控件当前尺寸各写各的（如 `.titlebar-btn` 46px、`.header-btn` 30x30、`.composer-send` 32x32）。请建立并落地一张**控件尺寸矩阵**：

- 图标按钮：定义 sm / md / lg 三档命中区（**最小 28x28，触达区不小于 32x32**），全部图标按钮只能取这三档
- 输入控件：统一高度、内边距、圆角、focus 态表现
- 弹层：统一宽度上限、`max-height`、`padding-top`、遮罩不透明度、阴影层级、圆角
- 列表项：统一行高、hover/active/selected 三态的**同一套** token
- 徽标（chip/pill）：统一高度与左右内边距

**同时统一状态表达**：`hover` 一律 `--bg-hover`、`active/selected` 一律 `--bg-active`、`disabled` 一律统一 opacity + `cursor: not-allowed`、`focus-visible` 一律 `--focus-ring`。当前这几个态在不同组件里写法不一。

### 2.5 现代感的具体着力点

「现代、好看」要落到可执行项，而非主观描述。参照 codex 桌面端：

- **留白节奏**：消息流左右留白与最大宽度（76ch）配合，避免宽屏下文字横贯全屏
- **层次靠背景与圆角，不靠边框**：减少 1px 边框的使用密度，用 `--bg-surface` / `--bg-elevated` 拉开层次
- **阴影只用于真正浮起的元素**（弹层、下拉），行内元素不加阴影
- **动效克制**：沿用 `--motion-in: 140ms` / `--motion-out: 100ms` + `--ease`；**必须实现 `prefers-reduced-motion` 全量降级为 0ms**（README 9.1 有要求，请核查是否真的落地）
- **空态、加载态、错误态三件套齐全**：这是"完成度感知"的关键。README 16.3 DoD 第 3 条已强制要求，请逐面板核查
- **滚动条**：统一自定义细滚动条样式，并注意 Windows 与 macOS 默认表现差异很大

---

## 3. 任务二：Windows / macOS 双套适配（自动检测）

### 3.1 平台检测的唯一来源

渲染层已可用 `window.agentdesk.platform`（`'darwin' | 'win32' | 'linux'`，preload 注入，`TitleBar.tsx` 已在使用）。

**硬性要求**：
- 平台判断**只允许**来自这一处。**禁止**在渲染层用 `navigator.userAgent` / `navigator.platform` 嗅探。
- 封装成一处工具（如 `utils/platform.ts`，导出 `isMac` / `isWindows`），不要在各组件里重复写 `=== 'darwin'`。
- `linux` 必须有确定归属（建议**与 Windows 同套**），不能落进未定义分支。

### 3.2 快捷键双套 —— 当前是真 bug，不只是显示问题

**已做的部分**：`utils/shortcut.ts` 已按平台改写**显示文案**（`⌘K` → `Ctrl+K`），`CommandPalette` 与 `Sidebar` 已接入。

**未做且必须修的部分** `[已验证]`：`use-keyboard.ts:12` 的按键**绑定**用的是

```
const mod = e.metaKey || e.ctrlKey;
```

即两个平台**不加区分地同时接受 Cmd 和 Ctrl**。这会产生两个方向的真实冲突：

- **macOS 上**：`Ctrl+K` / `Ctrl+N` / `Ctrl+P` / `Ctrl+A` / `Ctrl+E` 是系统级 Emacs 文本编辑绑定（删至行尾 / 下一行 / 上一行 / 行首 / 行尾）。当前实现会让用户在输入框里按 `Ctrl+K` 时**打开全局搜索而不是删除到行尾**。
- **Windows 上**：`metaKey` 就是 Win 键，而 `Win+P`（投影）、`Win+K`（连接设备）是系统占用。响应 metaKey 属于错误的按键语义。

**修法**：按平台取修饰键 —— macOS 用 `e.metaKey`，Windows/Linux 用 `e.ctrlKey`，不要用 `||` 混收。

**同时需要处理**：

1. **README 9.8 列出但代码中未实现的** `[已验证]`：`⌘⇧N`（新窗口）、`Esc Esc`（会话树 / fork，跟随 `doubleEscapeAction` 配置）。要么实现，要么从 README 移除 —— 不允许文档与实现继续背离。
2. **输入框内的快捷键作用域**：核查在 `textarea` / `input` 聚焦时，全局快捷键是否会错误抢占正常文本编辑（尤其 macOS 的 Ctrl 系、以及 `⌘A` 全选）。需要明确"哪些快捷键在输入态仍生效"。
3. **平台专属键位差异**：至少覆盖「设置」（macOS `⌘,` vs Windows 惯例）、「关闭窗口」、「最小化」、「全屏/最大化」、「撤销/重做」，以及 macOS 的 `⌘⌥` 与 Windows 的 `Ctrl+Alt` 组合可用性差异。
4. **Esc 守卫的连带风险** `[已验证，重要]`
   `use-keyboard.ts:104` 用 `document.querySelector('[role="dialog"]')` 判断"有弹层打开则不中止会话"。我核查了全部 10 个弹层组件，**当前它们都用 `role="dialog"`，所以这个守卫目前是有效的**。
   但 **README 9.9 明确要求「审批卡用 `role="alertdialog"`」**，而 `ApprovalModal.tsx:67` 现在是 `role="dialog"`。这意味着：**一旦有人按 README 修正这个 a11y 属性，Esc 守卫会立刻失效**，变成"审批弹窗打开时按 Esc 会中止整个会话"。
   → 修 a11y 时**必须同步**把守卫改为同时匹配 `[role="dialog"], [role="alertdialog"]`，或改用 store 中的弹层状态判断（更可靠，推荐）。请为此加一条回归测试。
5. **弹层 Esc 处理重复了 6 处** `[已验证]`：`ApprovalModal` / `CommandPalette` / `GlobalSearch` / `ContextUsageDrawer` / `SessionTreeOverlay` 各自 `window.addEventListener('keydown')`。多个弹层同时打开时 Esc 会**一起关闭**，且与全局 Esc 竞争。建议收口为一个统一的 `useDismissable` / 弹层栈，只关闭最顶层。

**验收**：README 9.8 的表格扩展为 macOS / Windows 两列，每一行都与 `use-keyboard.ts` 的实现一一对应；界面上展示的快捷键文案与当前平台真实可用的按键一致。

### 3.3 图标双套

**当前状态** `[已验证]`：`Icon.tsx` 只有**一套** 31 个图标（24 viewBox、`currentColor` stroke、`strokeWidth 1.75`）。用户要求分 Windows / macOS 两套。

**设计要求**：
- 两个平台的图标**语义名保持完全相同**（同一个 `IconName` 联合类型），只切换 path 数据 —— 调用方不感知平台。
- 风格取向：Windows 侧向 **Fluent** 风格（几何更方正、直角、线宽略粗），macOS 侧向 **SF Symbols** 风格（圆润、末端 round cap、线宽略细、光学重心偏轻）。
- **必须有回退**：某图标只做了一套时，自动回退到默认套，**不允许渲染空白**。请让类型系统或运行时断言保证「缺失即可发现」，而不是静默出空。
- 同时统一图标的**尺寸与描边**：目前调用点散落着 `size={12|13|14|15}` 等值，收敛到与 2.4 节控件档位对应的少数几档；线宽应随尺寸微调以保证光学一致（小尺寸下 1.75 会显糊）。
- **对齐**：图标与相邻文字的垂直光学对齐（`vertical-align` 或 flex + `line-height` 归一），这是"字挤/错位"观感的另一来源。

`[需你确认]` 我没有实测 macOS 上的实际渲染效果（开发机是 Windows）。两套图标的具体 path 需要你在实现后于两个平台各自核对，或至少在设计阶段就按上述风格差异产出。

### 3.4 窗口装饰与系统一致性

- **窗口控件**：Windows 的最小化/最大化/关闭在右上且为方形命中区；macOS 用左上交通灯（`trafficLightPosition` 对齐），且**不应自绘 mac 三键**。核查当前 `TitleBar.tsx` 在 macOS 下是否会与原生交通灯重叠。
- **自绘菜单栏**：README 9.1 要求「文件 / 编辑 / 视图 / 帮助」自绘菜单栏，**当前不存在**（`e8acffb` 只是移除了原生菜单）。请决策：实现它（Windows 侧内嵌于标题栏、macOS 侧走系统菜单栏）或修改 README 规格。**不允许维持"文档要求但没有"的状态**。
- **拖拽区**：确认 `-webkit-app-region: drag` 覆盖正确，且其上的按钮有 `no-drag`，否则按钮点不动（这是无边框窗口的经典缺陷）。
- 平台细节：滚动条外观、默认字体、焦点环表现、双击标题栏行为（macOS 缩放）、全屏语义差异。

---

## 4. 任务三：功能无 bug

### 4.1 系统性排查方法（不要只盯着看起来坏的地方）

上一轮的教训很具体：`Sidebar` 的搜索/通知按钮被写死 `disabled`，而 `GlobalSearch` / `CommandPalette` **早已完整实现**——功能是好的，只是入口被禁用；`CommandPalette` / `GlobalSearch` / `ContextUsageDrawer` 三个组件**从未有过任何 CSS**，打开就是裸元素。

这类缺陷靠"看截图"发现不了。请做**穷举式核查**，对每一个交互元素确认六件事：

1. 是否被无理由 `disabled`（全仓搜 `disabled` 并逐个确认理由）
2. 点击后是否真有 handler，且 handler 真的连到 store/IPC
3. **是否有对应的 CSS**（全仓交叉比对：每个 `className` 是否在样式文件里有匹配规则；反向也查一遍死样式）
4. 空态 / 加载态 / 错误态是否都有界面表达
5. 键盘可达（Tab 序合理）+ `aria-label`（README 9.9 强制）
6. 深色与浅色主题下都正常

同类地，请核查**所有 IPC 通道**：`InvokeMap` / `EventMap` / preload 三处是否同步（README 16.3 DoD 第 2 条），有没有"渲染层调了但主进程没实现"或反之。

### 4.2 已知待修项（本轮实测发现）

| # | 问题 | 状态 |
|---|---|---|
| 1 | `use-keyboard.ts` 修饰键不分平台（第 3.2 节） | `[已验证]` 必修 |
| 2 | README 9.8 有 `⌘⇧N` / `Esc Esc`，代码未实现 | `[已验证]` 必修 |
| 3 | Esc 守卫依赖 `role="dialog"` 选择器，与 README 9.9 要求的 `alertdialog` 冲突（第 3.2.4） | `[已验证]` 必修 + 加测试 |
| 4 | 6 个弹层各自监听 Esc，多层叠加时会一起关闭 | `[已验证]` 应收口 |
| 5 | `input`/`textarea`/`button` 的 `font-family` 不继承，需全局显式 `inherit` | `[已验证]` 必修 |
| 6 | `.npmrc-build-tmp` 误提交进版本库（已被 git 追踪） | `[已验证]` 需清理 |
| 7 | `apps/desktop/tmp-check-secret.cjs` 临时文件残留（未追踪） | `[已验证]` 需清理 |
| 8 | 17 个 Biome format 错误（清单见 1.5，均为历史遗留） | `[已验证]` 建议一次性格式化 |
| 9 | 255 处裸中文未走 i18n（`pnpm verify:i18n` 警告） | `[已验证]` 建议分批收口 |
| 10 | 对话发送失败报 `RPC prompt 失败`——根因是**未配置 provider / API 密钥**，上一轮只做了错误友好化与引导，未验证配好后的完整链路 | `[需你确认]` 请先配一个 provider 跑通端到端，再判断是否另有缺陷 |

### 4.3 测试要求

当前 41 文件 / 256 用例全绿，**不允许让它变红**。本轮新增逻辑必须补测试（README 16.3 DoD 第 1 条）：

- **平台分支必须双向测**：把平台检测做成可注入的，对 `darwin` 与 `win32` **各跑一遍**快捷键映射与图标选择。只测当前开发机平台等于没测。
- Esc 守卫（第 3.2.4）加回归测试，锁死"审批弹窗打开时 Esc 不中止会话"这个行为。
- token 一致性可以做成脚本断言（见第 6 节），把"不许硬编码"变成 CI 能拦住的规则，而不是靠人自觉。

---

## 5. 禁止事项

1. **禁止**为了让代码符合 README 9.1 的旧数值而回退 `e8acffb` 的视觉改动（见 1.3）。
2. **禁止**一次性重写 `styles.css` 或 `tokens.css`；按 2.1 的分组小步提交。
3. **禁止**在渲染层用 `navigator.userAgent` / `navigator.platform` 判断平台。
4. **禁止**新增硬编码颜色、字号、间距字面量；一律先加 token 再引用。
5. **禁止**用 `outline: none` 去掉焦点环（README 9.1）。
6. **禁止**为"看起来对齐"而设 `line-height: 1`（会压死 CJK，见 2.2）。
7. **禁止**给含中文的元素加负 `letter-spacing`。
8. **禁止**引入新的 emoji 作为 UI 图标（Windows 上字形退化，这是上一轮已清理掉的问题）。
9. **禁止**用非 UTF-8 编码或 CRLF 写入本仓库文件（见 1.6，已造成过不可恢复的内容损坏）。
10. **禁止**为了让 lint 通过而修改 `biome.json` 放宽规则。
11. 面向用户的字符串**禁止**硬编码，一律走 i18n，且 zh-CN / en 必须同时补齐（`verify:i18n` 会校验键数对称）。

---

## 6. 验收标准（可机械验证）

### 6.1 必须全绿

```
pnpm typecheck     # 目标：6/6 通过（当前已达成，不许退化）
pnpm test          # 目标：全部通过，且用例数 > 256（本轮应有新增）
pnpm verify:i18n   # 目标：zh-CN / en 键数对称；裸中文告警数应低于 255
pnpm lint          # 目标：0 error（当前 17 个历史遗留，本轮应清零）
```

### 6.2 量化指标（复现命令）

在仓库根目录执行（PowerShell，**注意用 UTF-8 读取**）：

```powershell
$u = New-Object System.Text.UTF8Encoding($false)
$t = [System.IO.File]::ReadAllText("apps\desktop\src\renderer\src\styles.css", $u)

# 硬编码字号（基线 151，目标 0）
[regex]::Matches($t, 'font-size:\s*[\d.]+px').Count

# 字号取值种类（基线 12，目标 <=6）
([regex]::Matches($t, 'font-size:\s*([\d.]+)px') | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique).Count

# 硬编码颜色（基线 34，目标 0）
[regex]::Matches($t, ':\s*#[0-9a-fA-F]{3,8}').Count

# 间距声明总数 / 其中走 var() 的（基线 322 / 1，目标：绝大多数走 var）
[regex]::Matches($t, '(?:gap|padding|margin)[a-z-]*:').Count
[regex]::Matches($t, '(?:gap|padding|margin)[a-z-]*:[^;]*var\(').Count

# 编码自查：必须为 0
[regex]::Matches($t, [string][char]0xFFFD).Count
```

**建议把前三项固化为 CI 脚本**（参考现有 `scripts/check-i18n.mjs` 的写法），否则欠债会重新累积。

### 6.3 人工验收清单

- [ ] Windows 与 macOS 各截图：标题栏、侧栏、会话流、Composer、三个弹层、设置页
- [ ] 每张图在**深色与浅色**下各一份，切主题无残留错色
- [ ] 中英文两种语言各一份，无文字溢出/截断/换行破版
- [ ] 中英混排处字距均匀、基线不跳（2.2 节）
- [ ] 全键盘走通一遍主流程：新建会话 → 输入 → 发送 → 审批 → 打开各弹层 → 设置，焦点环始终可见
- [ ] README 9.8 快捷键表逐条在**两个平台**实测可用
- [ ] 开启系统「减少动态效果」后，动效确实降为 0ms
- [ ] 窗口缩放到最小宽度不破版；高 DPI 缩放（125% / 150% / 200%）下字体不发虚

---

## 7. 工作流与交付

遵循 [建设步骤.md](建设步骤.md) 第 0 节的固定动作：

1. 完成一个分组 → 2. `git add` 本次涉及文件 → 3. Conventional Commits 提交（作用域用模块名，如 `style(tokens): ...`、`fix(keyboard): ...`）→ 4. `git push`（当前分支 `master`）→ 5. 确认成功再进下一组。

**一次提交只包含一个分组**，不要把 token 重构、平台适配、bug 修复混在一个 commit 里 —— 混合提交在出现回归时无法二分定位。

**交付物**：

1. `tokens.css`：完整的字号 / 行高 / 间距 / 圆角 / 层级阶梯
2. `styles.css`：0 硬编码字号与颜色，间距走 token
3. `utils/platform.ts`：平台检测唯一出口
4. `use-keyboard.ts`：按平台分流的修饰键 + 补齐缺失快捷键 + 弹层栈收口
5. `Icon.tsx`：双套图标 + 缺失回退
6. **README 同步更新**：9.1（回写实际数值、菜单栏结论）、9.8（拆成双平台两列）、9.9（a11y 落实情况）
7. 新增测试：平台双向分支、Esc 守卫回归、token 一致性脚本
8. 双平台 × 双主题 × 双语言的验收截图
9. 清理：`.npmrc-build-tmp`（需 `git rm --cached`）、`tmp-check-secret.cjs`

---

## 8. 一句话总结给接手模型

> M0–M9 的功能已经在了，**你的任务不是加功能，而是把 151 处散落字号、322 处散落间距、34 处硬编码颜色收进一套 token；把混排字体和被压死的行高修好；把只做了显示文案的平台适配做到按键绑定和图标资源；把每个交互元素按六态穷举核查一遍。**
> 判断标准不是"看起来还行"，而是第 6 节那些命令跑出来的数字，以及两个平台上真实按下去的每一个快捷键。
