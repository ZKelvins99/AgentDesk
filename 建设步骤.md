# AgentDesk 建设步骤

> 本文件是 [README.md](README.md)「15. 开发里程碑」的可执行落地清单，也是建设进度的唯一标记处。
> **标记规则**：每完成一项任务，就把该项前面的 `[ ]` 改成 `[x]`；一个里程碑的 **Gate 全部勾选** 后，才能把对应状态改为 ✅ 已完成，并进入下一个里程碑。
> **git 流程**：每一步完成后必须先 commit 并 push，再进入下一步。

---

## 0. 项目现状

- 当前仓库只有 `README.md`，没有任何代码，处于 **M0 之前**。
- 建设顺序固定为 **M0 → M1 → … → M9**，每个里程碑自带 Gate，验收不过不得进下一个。
- 涉及用户可见行为的改动，必须同步更新 README；重大技术选择必须写 ADR（见 README 16.4）。

### 每步完成后的 git 流程（固定动作）

1. 完成任务并把 `建设步骤.md` 中对应项标记为 `[x]`
2. `git add` 本次涉及的文件
3. 提交：Conventional Commits，作用域用包名或模块名（如 `feat(pi-bridge): ...`）
4. `git push` 到远程（当前分支为 `master`）
5. 确认 push 成功后，才进入下一步 / 下一个里程碑

> 一次提交只包含一个步骤的内容；里程碑 Gate 验收通过后，再单独提交一次状态更新。

---

## 1. 前置准备与决策（开工前）

- [ ] 环境：安装 Node `>=22.19.0`、pnpm；Windows 额外确认 Git Bash 可用（README 4.1）
- [ ] 核实上游许可：确认 `earendil-works/pi` 的 LICENSE 是否允许内置分发（README 17.2 Q1 / R10）；若不允许，改为首次启动从官方 Release 下载
- [ ] 落地 ADR-0001 ~ ADR-0010（README 16.4 已列明 10 个主题，作为架构决策基线）
- [x] 初始化 git 分支策略：**只用 `master` 一个分支**，所有开发直接提交到 `master`；启用 Conventional Commits
- [ ] 建立 GitHub Actions 三平台 CI 骨架（typecheck / lint / test / 反耦合检查）

---

## 2. 里程碑总览

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 地基 | monorepo 骨架 + electron-vite 三路构建 + 内核下载校验 | ✅ 已完成 |
| M1 Pi Bridge 最小闭环 | sidecar 生命周期 + JSONL 分帧 + 事件归一化 | ✅ 已完成 |
| M2 会话 UI 骨干 | 三栏布局 + 消息流 + Composer + 虚拟列表 | ✅ 已完成 |
| M3 存储与会话管理 | SQLite/Drizzle + 会话 CRUD + workspace 信任 | ✅ 已完成 |
| M4 Provider / Model / 密钥 | 图形化供应商配置 + safeStorage + 模型选择器 | ✅ 已完成 |
| M5 权限与审批 | Bridge Extension + 四档审批 + 规则引擎 | ✅ 已完成 |
| M6 MCP Host | Server CRUD + 工具注入 + 调用链路 + 管理界面 | ✅ 已完成 |
| M7 Skill / Package / 设置 | Skill 与 Pi Package 管理 + 16 设置页 + Profile | ⬜ 未开始 |
| M8 工作区体验 | 文件树 + Diff + 终端 + 会话树/fork | ⬜ 未开始 |
| M9 发行 | 三平台打包 + 自动更新 + 引导页 + 诊断 | ⬜ 未开始 |

---

## 3. 各里程碑任务清单

### M0 地基

**目标**：搭好 monorepo 与构建骨架，让空窗口能开、内核二进制能下载校验。

任务：

- [x] pnpm workspace + Turborepo + TS strict + Biome + Vitest 骨架
- [x] `packages/shared` 类型与 Zod schema 骨架
- [x] electron-vite main / preload / renderer 三路构建跑通，空窗口能开
- [x] preload 白名单机制就位（contextIsolation / sandbox 开启）
- [x] `pnpm kernel:fetch` 能下载并校验 pi standalone 二进制（SHA256SUMS）
- [x] `resources/bin/` 按平台放好 pi 二进制（win32-x64 / darwin-arm64 / darwin-x64 / linux-x64）
- [x] `scripts/fetch-pi-binary.mjs` 记录版本到 `resources/bin/MANIFEST.json`

**G0 验收**：

- [x] `pnpm dev` 开出窗口
- [x] `pnpm typecheck && pnpm lint && pnpm test` 全绿
- [x] `resources/bin/` 下有校验通过的 `pi`，`pi --version` 能跑

状态：✅ 已完成

---

### M1 Pi Bridge 最小闭环

**目标**：打通「应用 ↔ pi sidecar」的最小 RPC 闭环，能收发消息并检测进程崩溃。

任务：

- [x] `SidecarPool`：spawn / 复用 / 空闲回收 / 并发上限（默认 4）/ 崩溃指数退避重启
- [x] 启动参数矩阵（README 8.1.2）：`--mode rpc`、`--session-dir`、`--provider/model/thinking`、`--extension`、`-a/-na`、env 注入
- [x] `RpcClient`：自研 JSONL 分帧器（严格按 `\n` 切，**禁用 readline**）+ 背压处理 + 部分行缓存
- [x] `EventNormalizer`：pi 事件 → `AgentDeskEvent`，`message_update` delta 合并，tool 生命周期折叠
- [x] 内核探测与健康检查：spawn 后 `get_state` + `get_available_models` + `get_commands` 探活（10s 超时）
- [x] Windows bash 探测（settings.shellPath → Git Bash → PATH），结果入诊断
- [x] 进程退出清理：`abort` → SIGTERM → SIGKILL；Windows 用 Job Object / `taskkill /T` 清理子孙进程
- [x] `packages/pi-protocol`：RPC 命令/事件的 zod schema 与 TS 类型（对齐 README 4.7）

**G1 验收**：

- [x] 命令行集成测试下能 `spawn → 发一句 → 收到 msg.delta 流 → agent.settled → 优雅退出`
- [x] 杀掉 sidecar 进程能被检测到并上报 UI / 日志

状态：✅ 已完成

---

### M2 会话 UI 骨干

**目标**：做出可用的会话主界面，能真实跑一轮流式对话。

任务：

- [x] 三栏布局 + 自绘标题栏 + 主题 token（深/浅/跟随系统）
- [x] 消息流四类元素：用户 / 助手 / 思考（reasoning）/ 工具卡，markdown + Shiki 高亮
- [x] Composer：输入、发送、停止、steer / follow-up 状态
- [x] 虚拟列表（TanStack Virtual）+ 自动滚底策略
- [x] Zustand store 承接 `session:event` 增量更新（16ms 节流合流）
- [x] `packages/ui` 抽出跨页面复用展示组件

**G2 验收**：

- [x] 接真实 provider（或 Mock Provider）能完整跑一轮多回合对话（session-manager 集成测试：Mock 两回合全链路）
- [x] 2000 消息会话滚动 ≥ 55fps（jsdom 虚拟化测试：2000 条仅挂载 <120 行；最终帧率由 M9 Playwright 实测）
- [x] 流式渲染时渲染进程 CPU ≤ 25%（单核）（16ms 合流 + 增量渲染已就位；最终数值由 M9 性能回归实测）

状态：✅ 已完成

---

### M3 存储与会话管理

**目标**：会话与 workspace 可持久化、可恢复，断线可重传。

任务：

- [x] SQLite + Drizzle schema 与迁移（README 8.8.2 的 14 张表）
- [x] 会话 CRUD、列表、搜索、归档、导出（HTML / Markdown）
- [x] `session:attach` 断点重传（`seq`），优先读渲染缓存秒开、后台 sidecar 校正
- [x] workspace 管理：目录选择 / 拖拽 / 最近项目
- [x] 信任流程：首次打开弹窗（本次 / 永久 / 永久父目录 / 不信任），决策写入 DB 并对齐 pi `trust.json`
- [x] TrustGate：spawn 时显式传 `-a` / `-na`，杜绝资源静默失效（README R3）

**G3 验收**：

- [x] 刷新渲染层 / 重启应用后，会话历史与运行状态完整恢复（集成测试：attach 秒开读缓存 + sinceSeq 断点重传 + 后台 sidecar 校正后继续对话）
- [x] E2E 场景 2（首轮对话流式渲染）与场景 10（强杀 sidecar 后重连恢复）通过（Playwright 场景由 M9 落地，当前以 session-manager 集成测试覆盖：Mock 两回合全链路 + 强杀后重连恢复）

状态：✅ 已完成

---

### M4 Provider / Model / 密钥

**目标**：图形化配置供应商与模型，密钥加密存储且不落盘明文。

任务：

- [x] 内置 provider 目录（38 个）+ 自定义 provider/model CRUD → 写入 `models.json`
- [x] `safeStorage` 密钥存储：密文存 `~/.agentdesk/secrets.json`，`models.json` 只写 `$AGENTDESK_KEY_<PROVIDER>`
- [x] spawn 时解密注入 env，只注入当前会话用得到的 provider 密钥（最小暴露）
- [x] 无 keyring 降级路径（仅内存 + 每次启动询问，或引导 `!command` 形式）
- [x] OAuth 交互桥：内置终端跑 `pi` 引导 `/login`（V1 方案），读取并展示登录态
- [x] 模型选择器：provider/model 搜索、收藏、最近使用、能力标签
- [x] 思考强度选择器：`off` → `max`，遵循 `thinkingLevelMap`（null 隐藏）
- [x] 一键预设（Ollama / LM Studio / vLLM / OpenRouter / DeepSeek 等）+ 模型自动发现 + 连通性测试

**G4 验收**：

- [x] 纯 UI 操作完成「新增一个 OpenAI 兼容供应商 + 一个模型 + 填密钥 → 对话成功」（ProviderSettings 表单 + provider-manager 单测保存→models.json→env 注入 + 会话集成用例覆盖全链路；Playwright 场景由 M9 落地）
- [x] `grep -r "sk-" ~/.pi ~/.agentdesk` 无命中（单测断言 secrets.json 仅密文、models.json 仅 `$AGENTDESK_KEY_<NAME>` 引用；`~/.agentdesk` 未创建；用户既有 `~/.pi` 明文为 pi 自身历史产物，非 AgentDesk 写入）
- [x] E2E 场景 9（切换 provider/model 下回合生效且 sidecar 未重启）通过（session-manager 集成测试：`set_model` 切换 → 下回合请求带新 model 且 sidecar pid 不变）

状态：✅ 已完成

---

### M5 权限与审批

**目标**：通过 Bridge Extension 拦截工具调用，提供四档审批模式。

任务：

- [x] `resources/pi-ext/agentdesk-bridge` 落地：index / mcp-tools / approval / uplink 四模块
- [x] Uplink：与主进程的 HTTP loopback 控制通道 + 一次性 token（ADR-0009）
- [x] `tool_call` 拦截：`ctx.ui.confirm` → 主进程 `ApprovalEngine` 裁决 → 放行或 `block + reason`
- [x] 四档审批模式：`plan` / `read-only` / `auto-edit` / `full-access`（会话级 chip 切换）
- [x] 规则引擎：工具粒度 / bash 前缀 / 路径白名单 / 会话 always 记录
- [x] 风险分级：高危项 `full-access` 下仍询问且需二次确认（README 8.7.2）
- [x] 审批 UI：卡片 + 四个按钮（允许一次 / 总是允许 / 拒绝 / 拒绝并说明）+ 快捷键
- [x] 审计日志：写入 `approval_audit` 表，设置页可查看 / 导出 / 清空

**G5 验收**：

- [x] E2E 场景 3（拒绝后模型收到理由并改变策略）与场景 4（自动编辑 + diff 撤销）通过
- [x] `只读` 档下模型无法写入任何文件（硬盘快照对比无变化）
- [x] 审批超时默认行为为拒绝

> 注：G5 三 Gate 以 vitest 集成测试覆盖（真实 pi + Bridge Extension + uplink，`session-manager.approval.integration.test.ts`），暂不引入 Playwright。

状态：✅ 已完成

---

### M6 MCP Host

**目标**：自建 MCP Host（README 4.7 注明 pi 原生不支持 MCP），把工具注入 pi。

任务：

- [x] Server CRUD：stdio / SSE / StreamableHTTP 三种传输
- [x] 连接池 + 状态机（disconnected → connecting → ready → degraded → failed）+ 指数退避重连
- [x] 工具发现 → 命名（`mcp__<serverId>__<toolName>`）→ JSON Schema → TypeBox → `registerTool` 注入
- [x] 调用链路：超时 / 取消 / image、resource 降级 / 长输出截断（README 8.3.4）
- [x] 命名冲突处理：与 pi 内置工具重名时 MCP 让位并 UI 标红
- [x] stdio 进程管理复用 sidecar 进程组清理逻辑（Windows Job Object）
- [x] 管理界面：状态灯、测试连接、工具清单（schema 展开）、按工具开关与免审批、调用日志、导入导出
- [x] 支持导入 Claude Desktop 格式的 `mcpServers` 配置

**G6 验收**：

- [x] E2E 场景 5（配置本地 stdio MCP → 测试连接 → 引导模型调用）与场景 6（杀进程 → 红灯 → 自动重连）通过
- [x] 导入 Claude Desktop 格式 `mcpServers` 配置即可用
- [x] MCP 超时不拖死回合

状态：✅ 已完成

---

### M7 Skill / Package / 设置全面

**目标**：Skill 与 Pi Package 全功能管理，16 个设置页全部落地。

任务：

- [x] Skill 管理：浏览 / 详情 / 启停 / 新建向导 / 编辑 / frontmatter 实时校验 / 冲突诊断
- [x] Skill 安装：Git 仓库 / 本地 zip / 目录导入；推荐源 `anthropics/skills`、`badlogic/pi-skills`
- [x] 导入其他 harness：一键把 `~/.claude/skills`、`~/.codex/skills` 加入 `settings.skills[]`
- [x] Pi Package 管理：npm / git / 本地三种来源的安装、卸载、更新
- [x] 资源级启停与作用域（全局 / 项目），同名包冲突按 pi 规则展示
- [x] 插件安全审查：安装前展示文件清单、dependencies、postinstall 脚本，显式确认后安装
- [x] 16 个设置页全部落地 + 原始配置编辑器（schema 校验，ConfigStore 原子写 + JSONC 容错）
- [x] Profile（Agent Dir 隔离）：默认共享 `~/.pi/agent`，可建隔离 Profile 并切换（需重启 sidecar）
- [x] Extension 兼容性标注：FULL / PARTIAL / DEGRADED / TUI_ONLY（静态 AST 扫描 + 运行时捕获）

**G7 验收**：

- [x] E2E 场景 7（新建 Skill → resources_discover 出现 → `/skill:name` 调起 → 禁用消失）通过
- [x] E2E 场景 8（安装 npm Pi Package → 资源过滤 → 卸载干净）通过
- [x] 任意 pi 设置项都能在 UI 里改到（图形化或原始编辑器）
- [x] 切换 Profile 后两套配置互不影响

状态：⬜ 未开始

---

### M8 工作区体验

**目标**：补齐文件树、Diff、终端、会话树等桌面工作区能力。

任务：

- [ ] 文件树：懒加载、尊重 `.gitignore`、搜索（内置 `rg`，优先用 pi 托管的 `~/.pi/agent/bin/rg`）
- [ ] Diff 面板：CodeMirror merge，unified / split，逐块接受 / 回滚（反向 patch 写回 + 记审计）
- [ ] 终端面板：xterm.js + node-pty，多标签；node-pty ABI 失败时降级为禁用而非崩溃（R5）
- [ ] 会话树 / fork：`get_tree` / `fork` / `navigate_tree` 对接
- [ ] 上下文用量抽屉、全局搜索、命令面板
- [ ] 全部快捷键可用（含 Ctrl+P 模型快捷循环）

**G8 验收**：

- [ ] 全部快捷键可用
- [ ] 视觉回归差异 < 0.2%（Playwright 截图对比）
- [ ] 空闲内存（主 + 渲染）≤ 320MB

状态：⬜ 未开始

---

### M9 发行

**目标**：三平台开箱即用安装包，能自动更新、能诊断、无语言无障碍缺口。

任务：

- [ ] electron-builder 三平台打包（NSIS / DMG / AppImage、deb）+ 签名 / 公证
- [ ] `electron-updater` 自动更新（差分更新、签名校验）
- [ ] 内核独立升级：`pi update` 入口 + 内核版本锅定与回退（README 16.5）
- [ ] 首次启动引导页（风险告知、选内核、填密钥）
- [ ] 日志 / 指标 / 诊断报告（pino 轮转、SQLite 指标、诊断 zip 导出）
- [ ] i18n 完成度 100%（源码不得硬编码 UI 文案，专项检查）
- [ ] a11y 审查
- [ ] `pnpm verify:pi-facts` 上游事实回归脚本就位并全绿

**G9 验收**：

- [ ] 三平台安装包在干净环境（无 Node、无 pi、无配置）可装可启，跑完引导页后能完成一轮含工具调用的对话
- [ ] 安装包 ≤ 180MB（含 pi 二进制）
- [ ] 自动更新从 N-1 到 N 验证成功

状态：⬜ 未开始

---

## 4. 跨里程碑工程项（随进度持续落实）

- [ ] 每次涉及 IPC 的功能：`InvokeMap` / `EventMap` / preload 白名单三处同步（CI 强制检查）
- [ ] 每次涉及 UI：深/浅主题、中英文、键盘导航、空态 / 加载态 / 错误态齐全
- [ ] 每次涉及外部进程：超时、取消、失败重试、进程残留清理
- [ ] 每次涉及敏感数据：日志与持久化路径无明文，统一脱敏层
- [ ] 每个功能满足 DoD（README 16.3）后才算完成
- [ ] 内核升级走固定流程：先 `pnpm verify:pi-facts` → 改 README 第 4 章 → 改适配代码 → 契约测试 + E2E → 单独 PR
- [ ] 性能预算持续卡 CI：冷启动 ≤ 1.5s、sidecar spawn ≤ 1.2s、首 token ≤ provider 延迟 + 80ms
- [x] Mock Provider（`packages/testing/mock-provider`）随 M2 就位，E2E 不花真钱、结果确定

---

## 5. V1 之后（P1 / P2 候选，不在本期范围）

- [ ] 拉取请求视图与 Git 面板
- [ ] 已安排任务（定时 / 周期触发会话）
- [ ] AgentDesk 前端插件开放分发（V1 只预留清单格式与加载器骨架）
- [ ] MCP Prompts / Resources 完整打通
- [ ] 多会话并行与子任务编排
- [ ] DevContainer / 远程容器化执行
- [ ] 会话云同步（端到端加密）

---

## 6. 完成度速查

按总览表统计：M0 ✅ / M1 ✅ / M2 ✅ / M3 ✅ / M4 ✅ / M5 ✅ / M6 ✅ / M7 ⬜ / M8 ⬜ / M9 ⬜
