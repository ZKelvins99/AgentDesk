{
  "name": "@agentdesk/runtime-protocol",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "AgentDesk Runtime/Event/Capability/Artifact 协议类型（零依赖）",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "bun build src/index.ts --outdir dist --format esm --target node",
    "test": "node --test tests/*.test.ts"
  }
}