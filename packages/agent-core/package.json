{
  "name": "@agentdesk/agent-core",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "AgentDesk Agent Registry（M18）：Agent 与 Runtime 概念分离、AgentDefinition、默认 Agent。",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test tests/*.test.ts"
  }
}
