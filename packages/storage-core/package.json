{
  "name": "@agentdesk/storage-core",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "AgentDesk Workspace/Storage Core（M10）：node:sqlite 本地数据库、workspaces、session 映射、runtime 配置、崩溃恢复。",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test tests/*.test.ts"
  }
}
